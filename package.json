{
  "name": "raml",
  "private": true,
  "version": "1.0.0",
  "description": "Raml — minimalist Persian text RPG PWA",
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "test": "npm test --prefix backend",
    "build": "npm run build --prefix frontend"
  }
}
